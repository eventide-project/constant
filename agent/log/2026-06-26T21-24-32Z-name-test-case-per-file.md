# Constant#name is tested with a case per file under test/automated/constant/name/: nested.rb (a nested constant — #name strips the namespace path to the final segment) and top_level.rb (a top-level constant — #name is the whole single-segment name). When a Constant-class feature has distinct input shapes that exercise different paths, give each its own file under a feature directory, mirroring import_constant/
