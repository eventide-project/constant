# Rule: a test file's context nesting mirrors its folder hierarchy — each directory segment under test/automated/ becomes a context (e.g. constant/name/ -> "Constant" > "Name"), not a flattened combined context ("Constant Name"); matches the existing import_constant/macro convention. Surfaced when the flattened nesting in the Name tests was caught after Run 1 had already been integrated
