# Task 5: `Constant.build(raw_constant)` — the constant-object arm of the developer-facing constructor — delegates to `new`. Confirmed (via the design doc) that this arm earns its existence even though it duplicates `new`: `build` is the single polymorphic public constructor (value arm now, name-resolution arm in Task 6) that owns all convenience + validation, while `new` stays the bare no-validation state-recorder. Kept Task 5 (value) and Task 6 (name) separate. Test asserts `build(value) == Constant.new(value)` (value-equality form, mirroring the namespace test, over reaching into `.raw_constant`). One outcome, file named for the feature (`build.rb`); outcome context named at the close: "Is a Constant for the value". Param kept as `raw_constant` (honest to this arm; Task 6 generalizes it). 37 tests green.
