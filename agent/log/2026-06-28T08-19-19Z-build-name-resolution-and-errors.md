# Task 6 (in progress): `build` grows the name-resolution arm. `build(name_or_raw_constant, namespace = Object, inherit: false)` — value arm delegates to `new`; name arm resolves via `const_defined?`/`const_get` (threading `inherit`), wrapping the resolved module/class. Introduced `Constant::Error = Class.new(RuntimeError)`, raised from build's two error branches with distinct messages: undefined name → "NAME is not defined in NAMESPACE"; resolves to a non-module value → "NAME in NAMESPACE is not a module or class". Discriminator `is_a?(Module)` (accepts module or class since `Class < Module`). Pulled Task 9's controls extension forward: `Controls::Constant.example` now seeds non-module inner constants via a Hash form (`inner_constants: { name => value }`), Array form unchanged. `build.rb` became the `build/` folder (value, name, undefined, non_module cases). Param named `name_or_raw_constant`. Remaining Task 6 outcomes: defaulted-namespace and `inherit`. 40 tests green.
