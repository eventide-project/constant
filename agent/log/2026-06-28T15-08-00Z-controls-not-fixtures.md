# Terminology: say "controls", never "fixtures" — the example constants a test is built from, and the `Controls::Constant` helpers that build them, are *controls* (Eventide/TestBench vocabulary); "fixture" is imported xUnit jargon (and was already rejected once as a candidate handle). Fixed the four genuine misuses just introduced (three-hinge rule ×3, digest ×1); left the digest's historical line that *quotes* "fixture" as a rejected word. → rule `…15-08-00Z-controls-not-fixtures.md`
