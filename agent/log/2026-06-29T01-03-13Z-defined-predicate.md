# Task 7: `Constant.defined?(name, namespace_name_or_module=nil, inherit: nil)` — the class-level definition predicate — reflects `build`'s signature (positional `name`; positional namespace as name-or-module; trailing `inherit:`) and reuses `build` to resolve the namespace, then `const_defined?`. The namespace default is **required** (the namespace lands in `build`'s first, undefaulted slot, so `build(nil)` raises `TypeError`); the `inherit ||= false` default is technically redundant (`const_defined?(name, nil)` already acts as `false`, confirmed by a discriminating ancestor test) but **kept to mirror `build`**. One driven outcome — `Affirms the constant name is defined in the namespace`; the false / type-agnostic / default-namespace / `inherit` facets were green-on-arrival and dropped. Feature context named "Defined Predicate" (predicate-context rule). 42 green. → plan Task 7 checked.
