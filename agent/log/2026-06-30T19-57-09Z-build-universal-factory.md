# Constant.build is now the universal factory: a name resolving to a module yields a Constant::Module, a name resolving to a literal yields a Constant::Literal (name, value, namespace); the non_module error is removed (resolving to a literal is no longer a Constant::Error); the undefined-name error remains. Name not yet normalized to String (.to_s) — deferred to the Symbol-name outcome
