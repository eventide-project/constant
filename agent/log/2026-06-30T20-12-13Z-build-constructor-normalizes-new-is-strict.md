# Each domain class gets both new (strict initializer, records inputs as-is) and build (forgiving constructor, normalizes inputs then delegates to new); Constant::Literal.build coerces a Symbol name to String, Constant::Module.build is near-passthrough (symmetry); the top-level Constant.build factory delegates to the subtype builds so normalization flows through every construction path
