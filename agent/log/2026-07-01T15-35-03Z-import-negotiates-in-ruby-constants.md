# Settled: Constant::Import negotiates in raw Ruby constants, not Constant instances — it takes and returns Ruby constants (modules/classes), the way a native Ruby import would. Import stays in Ruby's own currency, independent of the Constant domain object (this is the current behavior, now a deliberate decision; no code change). Resolves and removes the import-negotiate-in-constant-class deferred item
