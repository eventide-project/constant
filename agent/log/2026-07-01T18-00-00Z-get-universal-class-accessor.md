# Settled: `Constant.get` is the sole universal class-level accessor; `Constant.build` (the class-level factory) is removed. `Constant.get(value, namespace=Object, inherit:)` takes a module (or class) and returns the mediating `Constant::Module`, or takes a name and resolves it in the namespace — the class-level form of the instance `#get` (namespace passed as an argument rather than being `self`). Because `Constant.get(mod)` does exactly what `Constant.build(mod)` did, the class-level `build` was a duplicate entry point, not a distinct operation, so it is dropped. Construction from a value you already hold is the subtype constructors `Constant::Module.build` / `Constant::Literal.build`. The `Constant()` coercion is thinned to a veneer over `get`, carrying only its own three coercion concerns: idempotence (already-`Constant` → itself), the nil/non-coercible **type guard** (`TypeError: can't convert nil into Constant`), and delegating everything else to `get`. `defined?` also routes its namespace normalization through `get`. Rationale: keep the refinement surface minimal and put the substantive logic in a plain, always-available class method. Behavior-neutral; `build/value.rb` folded into `get/value.rb` and the `build/` test directory removed. Initializers untouched. Suite green (68). Supersedes the earlier "`build` as universal factory" and the brief `build`/`get` split. Loop record: agent/loops/2026-07-01-get-universal-class-accessor.md
