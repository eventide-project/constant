# Built: nested-path (`"Foo::Bar::Baz"`) resolution. The `::`-split lives in the instance `Constant::Module#get`, which splits on `::` and **recurses** — `get(head).get(rest)` — so each hop is a single-segment resolution against the parent it actually resolved through. `Constant.get` (class) and the `Constant()` coercion inherit path support by delegating to the instance primitive. Settled sub-questions: (1) **inherit** applies at every hop (each recursive `get` receives the same `inherit`); (2) a terminal **literal** is built from a genuine final segment with its true enclosing namespace — fixing the malformed-literal that native `const_get("a::b")` produced (compound `name`, shallow `namespace`, only a coincidentally-correct `full_name`); (3) a **mid-path literal** errors correctly — `Constant::Literal#get` raises; (4) the **error message** names the exact failing segment and its parent, since each hop is single-segment. Outcomes (instance `module/get/`): nested literal (name = final segment; namespace = enclosing module), nested module (resolves to the Constant), undefined final segment (error), mid-path literal (error). New controls helper `Controls::Constant::Nested.example(inner_name:, leaf_name:, leaf_value:)` builds a genuinely-nested `NS::Inner::leaf` (inner first-bound inside its parent so Ruby assigns the nested name). NOT in scope: `Constant.defined?` path support (leans on native `const_defined?`, untested), leading `"::"` (unsupported). Resolves the nested-path-strings deferred item. A separate deferred item queues a `Constant.get`-level demonstration test. Suite green (73). Loop record: agent/loops/2026-07-01-nested-path-strings.md
