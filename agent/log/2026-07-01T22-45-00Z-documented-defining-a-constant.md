# Documented "Defining a Constant" in the README, replacing the "To Do" placeholder. `Constant::Define.(constant_name, receiver_constant)` creates a new module, assigns it to `constant_name` in the receiver constant's namespace, and returns the newly-defined constant (it is the mechanism `Constant::Import` uses to create an alias target). The section mirrors the `Constant::Import` API style — intro, Example, API (signature + `.call` example), Returns, Parameters table. Define has no macro, only the `.call` form, so the section documents that alone. Kept the section at the end (after "The Constant Class", before License) per the developer's placement choice. Documented example verified against the real API (Symbol name form). Docs only — no code, no test impact. Resolves the document-defining-a-constant deferred item
