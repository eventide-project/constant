# Categorized the 67 flat `agent/rules/` files into eight subdirectories — organization only, no rule content changed. Taxonomy (settled with the developer, who chose subdirectories over an index document): `terminology/` (16 — canonical craft terms and say-X-not-Y de-slang), `methodology/` (20 — the efferent-oriented-design process: hinge cycle, gates, human-in-the-loop, what's-worth-testing, hinge presentation), `test-writing/` (11 — TestBench test-code structure, naming, control variables), `code/` (7 — production Ruby style), `commands/` (5 — invokable reports/commands), `process/` (3 — agent file naming, loop records, dialogue capture), `git/` (3 — commit conventions and the pre-commit workflow), `docs/` (2 — plan/design-document conventions). Each file kept its ISO 8601 UTC datetime prefix and slug; only its directory changed (`git mv`, rename-detected). Rules cite each other two ways — by **name** (survives a move) and by **path** — and the 35 path references across 25 files (rules citing rules, plus `AGENTS.md`, `agent/log/`, `agent/observations/`, `agent/experiments/`, `agent/sessions/`) were repointed `agent/rules/<slug>` → `agent/rules/<category>/<slug>`, verified with zero dangling flat references and every repointed path resolving to a real file. The deferred item's open questions were settled: representation = subdirectories; the UTC prefix is kept within each category directory; historical records were repointed too (a rule's path is not historically meaningful — same rule, new location). Out of scope and left for a later item: consolidating the terminology **near-duplicates** (e.g. `controls-not-factories` / `controls-not-fixtures` / `say-control-not-arrange` all assert "controls"; `actuation-not-call` / `tdd-efferent-not-caller` / `tdd-lexicon` overlap) — categorizing surfaced them but did not merge them. Resolves and deletes the `categorize-the-rules` deferred item.
