# Amended the optional-params-default-in-body rule: a settable payload parameter that may be legitimately falsy defaults via `if .nil?`, not `||=` — codifying `Constant::Define.call`'s existing form as the convention's boundary (`||=` for flags/selectors whose own falsy default is the only falsy meaning; `.nil?` for a value the method stores/forwards verbatim, where a caller's `false`/`nil` must survive).
