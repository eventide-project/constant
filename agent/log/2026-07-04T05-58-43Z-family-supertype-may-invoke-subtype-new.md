# Ruled review finding #2 an allowed exception, not a cleanup: within the `Constant` family the supertype acts as a factory of its subtypes and may invoke a subtype's `new` directly (e.g. `Constant.namespace` calling `Constant::Module.new`) when it already holds the strict, normalized input — skipping `build`'s determination logic, which is a no-op there. Amended the build/new-strict rule with the intra-family privilege and its boundary (family code, own subtypes, input already strict; anything needing normalization still uses `build`, and external callers always do). Removed #2 from the queued deferred item, leaving only #3 (the `Constant.defined?` explaining-variable touch-up).
