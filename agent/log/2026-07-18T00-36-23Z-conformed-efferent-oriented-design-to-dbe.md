# Ran the deferred `conform-efferent-oriented-design-to-dbe` sweep and deleted the deferred item. Conformed the one live use of the retired methodology *name*: `first-implementation-may-run-a-contained-red-green-loop` said "(efferent-oriented design — new behavior…)" → "(Design By Efferent — …)". Left `tdd-as-design-tool`'s "efferent-oriented, high-solubility code" — that is the adjective for *code* (designed from the efferent side), not the methodology's name, which the DBE rule's scope note explicitly excludes. Repointed the DBE rule's own "conformed in a later pass (see the queued deferred item)" to "conformed on sight" now that the pass is done. The DBE rule still names "efferent-oriented design" where it defines the retirement — that stays. Historical `agent/log/` entries keep their words.
