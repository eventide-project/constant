# De-jargoned the gate-forecasting terms: "mean-bias" replaces "anchoring" (names the direction — pulling the human toward the AI's mean-drawn answer — and isolates the harmful kind of bias from benign bias toward an above-the-mean answer); "miss / false alarm" replaces "recall / precision", and "lean toward flagging" replaces "recall-biased". Swept through the gate-forecasting observation, the dialogue digest, and prior log entries
