# Extracted the `name`/`namespace` calculation into static methods receiving a `raw_constant` (deferred item, now deleted). `def self.name(rc)` / `def self.namespace(rc)` hold the `rpartition("::")` logic; instance methods are thin delegators. The `Class#name` collision (a class method `name` shadows `Class#name`, raising on no-arg `Constant.name`) was resolved by the human's dictated shape: `class << self; alias_method :__name, :name; end` preserves native via `Constant.__name` (matches the `__import_constant` underscore convention) and frees `name` for the calculation. Decisions: class methods get **no separate tests** (instance tests cover them via delegation); instance→class delegations always via **`self.class`** (subclass-aware); class method builds wrappers with **bare `new`** (`self.new` superfluous in a class method). `full_name` left inline (out of scope). 36 tests green.
