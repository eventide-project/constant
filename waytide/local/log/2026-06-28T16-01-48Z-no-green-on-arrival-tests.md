# TDD principle: drop an outcome whose test is green-on-arrival (passes the moment written) — it drives no design, only confirms an existing affordance. The tell is a **no-op implementation hinge** (hinge 4 has nothing to write). Surfaced live: the defaulted-namespace outcome's test passed against existing code (the `namespace = Object` default and `#namespace`'s top-level handling were written earlier, during the name-resolution outcome) — so the human dropped it. Green-on-arrival usually means the implementation ran *ahead* of its driving test. Locking such behavior as a regression guard is a separate, deliberate human decision, not a TDD step. Consistent with exit-on-solubility-not-coverage. → rule `…16-01-48Z-no-green-on-arrival-tests.md`
