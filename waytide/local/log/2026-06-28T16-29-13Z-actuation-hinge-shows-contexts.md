# Process: at the actuation hinge (hinge 1), show the actuation inside its nested folder-mirroring `context` blocks (e.g. `context "Constant"` / `context "Build"`); downstream hinges (assertion, controls) omit the contexts and show only their concern + the actuation anchor. The nesting (structural placement) is decided once, at hinge 1, then elided as settled. Third of the per-hinge presentation rules. → rule `…16-29-13Z-actuation-hinge-shows-contexts.md`
