# Naming: suffix a namespace/constant control variable by the form it holds — raw constant = bare (`control_namespace`), name = `_name` (`control_namespace_name`), Constant object = `_constant` (`control_namespace_constant`). And an extracted expected operand is a control (`control_` prefix, never `other_`): `other_constant`/`other_namespace` → `control_constant` / `control_namespace_constant`; the under-test result stays unprefixed (`constant`, `namespace`). Applied across build/ and namespace/ (41 green); the broader Constant-class tests (name/, full_name, equality/) still use control_module/control_inner_constant_name and want the same sweep; import_constant left (separate domain, non-control vars). → rule `…17-11-02Z-namespace-variable-suffix.md`
