# Two conventions: (1) the over-wrapped-test flattening applies only when there's no code between the context and the test AND the context is not a leading "When ..." condition — an outcome name that merely contains "when" (e.g. "Equal when mediating the same module") stays a flat named test; (2) new "test tree" command — run the suite and print a normalized context/test tree with duplicate outer context headings de-duped (merged)
