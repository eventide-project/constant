# Dropped: the instance `#define` / `#import` path on a `Constant` (the mutating siblings of `#get`). Discontinued as a direction and removed from the frontier queue — the `Constant` object is not being extended into a mutation handle on its module. `Constant::Import` remains as-is, negotiating in raw Ruby constants (per the 2026-07-01 settlement), independent of the `Constant` domain object. Removes the instance-define-import deferred item
