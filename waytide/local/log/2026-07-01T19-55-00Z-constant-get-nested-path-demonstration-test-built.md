# Built: the `Constant.get`-level nested-path demonstration test (`test/automated/constant/get/nested.rb`). Drives a `::`-path through the public class front door — `Constant.get("SomeInnerNamespace::SomeDeepNamespace", namespace)` — and asserts it resolves to the mediating `Constant::Module`. Green-on-arrival (the instance `#get` recursion already does the work; `Constant.get` inherits it by delegation), but it guards the normalize-and-delegate hop from `Constant.get` down to the instance primitive — a regression there would otherwise pass every instance test while the front door silently broke. Single outcome, named "Is a Constant mediating the nested module". Resolves the constant-get-nested-path-demonstration-test deferred item. Suite green (74)
