# Folded `Controls::Constant::Nested` into the base `Controls::Constant.example` and dropped the dedicated helper. `add_inner_constants` now **recurses on a Hash value**: a Hash-valued inner constant means "nest deeper" — create the inner module, bind it into its parent **first** (so Ruby assigns the correct nested `.name`), then recurse. So `example(inner_constants: { "Inner" => { "Leaf" => value } })` builds `NS::Inner::Leaf` to any depth, and a non-Hash leaf (a literal or a module you pass) is `const_set` directly. Verified safe: no existing fixture used a Hash as a *value*, so no collision. Switched `module/get/nested_literal.rb`, `module/get/nested_module.rb`, and `get/nested.rb` off `Nested.example` onto the base control; removed `lib/constant/controls/constant/nested.rb` and its require. The `leaf_value` parameter-naming question is now moot (the helper is gone). Behavior-neutral; suite green (74). Resolves the revisit-nested-control deferred item
