# Introduced an explaining variable in `Constant.defined?` (review finding #3): binds `namespace_constant = get(namespace_name_or_module)` before `namespace_constant.get(name, inherit: inherit)`, matching the explaining-variable convention used in `#constants`/`#constant_names` and `Constant::Module#defined?`. Behavior unchanged; suite green at 92/92. Resolves and deletes the last queued deferred item; the deferred queue is now empty.
