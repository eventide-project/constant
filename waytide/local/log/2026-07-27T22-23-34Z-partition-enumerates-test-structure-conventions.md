# The step-local partition must explicitly enumerate test-structure conventions — context nesting mirrors folders, the "Is" naming rule, control_ prefixing — as auditable items. The name-feature experiment's one true miss (flattened context nesting) escaped the forecast, every gate, and integration because it was never itemized at all: exposing the proceed-pile only helps for items that are in the pile, so a gap in the partition is invisible to the audit
