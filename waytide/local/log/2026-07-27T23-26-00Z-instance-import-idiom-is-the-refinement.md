# The instance-destination idiom is `using Constant::Import` and then `some_object.import(SomeOrigin)`, superseding `include Constant::Import::Macro` (2026-07-26T21-36-30Z-instance-import-idiom-is-supported), which is withdrawn now that `Macro` is unpublished. The behavior is unchanged — the refinement on `::Object` reaches any receiver and `Import.call` still normalizes a non-module destination to its class
