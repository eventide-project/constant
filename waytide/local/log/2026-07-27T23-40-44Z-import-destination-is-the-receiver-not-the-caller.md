# The import destination is omitted at the use site by making the caller the refined method's receiver (`using Constant::Import`), not by deriving it from the caller's binding — that mechanism is impracticable in pure Ruby: TracePoint yields the callee's binding, the one recovery that works fires after the call returns and silently imports into the wrong destination when the import is a module body's last statement, and a globally-armed shadow stack costs 8.8x on every method call in the process
