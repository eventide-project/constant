# Declining the recommended verdict is what produced the import refinement: the import-destination experiment was gated as Refuted after gate 1 closed caller-binding derivation, and the working answer was one probe away — the verdict had been scoped to the mechanism under test rather than to the experiment's motivating question. When a gate resolves negative, check whether it refutes the question or only the first candidate mechanism
