# Every reduction in the import refinement came from probing an assumption rather than reasoning from it: `refine ::Module` was dropped once measured (`refine ::Object` reaches modules and classes, since they are objects), the separate `Refinement` namespace was dropped once questioned (`using Constant::Import` works directly), and `private_constant :Macro` was called foreclosed on an unchecked inference that the README's documentation made Macro published
