# The `macro/top_level.rb` test keeps its path and contexts but changes subject to the refusal, asserting both that top-level inclusion is refused and that the error names the refinement as the remedy — the idiom it had covered no longer exists. `Controls::Script::Example` gains `#error`, which runs the script and returns its error output, raising if the script unexpectedly succeeds; `#run` and `#error` share a new `#execute`
