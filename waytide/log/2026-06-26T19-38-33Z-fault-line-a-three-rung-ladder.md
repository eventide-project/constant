# Fault-line A's structure is a three-rung ladder: (1) reached — the AI reached it (produced and surfaced), on the list; (2) missed — the AI could reach it but missed this pass, fixable by more/varied lists; (3) unreachable — only the person can produce it, not fixable. It is a degenerate 2×2 (reachable/not × surfaced/not) whose fourth cell is impossible because reached presupposes reachable (you can't surface what you can't produce), so unreachable options are always off the list. Fault-line A lives in rungs 2-3. Labels settled as reached / missed / unreachable
