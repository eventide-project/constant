# Terminology: no slang — "wrap"/"wrapper" → "mediates" (a Constant mediates a module), "sweep" → "conform" (also "reconcile"/"propagate"); and the canonical relation verb is the bare "mediates", not "mediates for" (supersedes the wording in the `…22-39-10Z-mediates-for-verb.md` log and the `…06-33-29Z-no-prepositions` rule's example; "sent to" unaffected). Literal naming over figurative — the prose/process counterpart of name-literally-not-by-analogy, and the same impulse as reserving "constant" for Constant instances / "module"/"mod" for the raw module. Historical records keep the word they quote; live prose (design doc, plan, a session, the `agent/deferred/…control-comment-prefix.md` item) conforms in a deferred pass. → rule `…23-05-57Z-terminology-no-slang-mediates.md`
