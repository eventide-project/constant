# Added `Constant#raw` as an alias for `#mod` (the raw-module reader) — a shorter, adjectival accessor for the same value. Mechanical alias, no test (consistent with the untested `#mod` initializer reader and the `__name` alias precedent). Used the **`alias` keyword** (`alias raw mod`), not `alias_method`: verified the keyword form behaves identically — including for the inherited `Module#name` aliased inside `class << self` — so also converted the existing `alias_method :__name, :name` → `alias __name name` for consistency and terseness. 42 green.
