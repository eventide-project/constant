# Equality protocol lifted into the Constant mixin over a single subtype hook named `identity` (not the coined `equality_key`): == compares identity, eql? delegates, hash is identity.hash; Constant::Module#identity is #value, Constant::Literal#identity is #full_name (equality by binding location). `identity` chosen as a literal name (the constant's identity); `equality_key` rejected as invented/non-conventional
