# Conformed error tests to the "Is an error" + condition-context form (per the assert-raises rule): build/undefined.rb (condition promoted to `context "When …"`, test named "Is an error"), and the legacy import error tests (already_included "Raises error" and alias "Not defined" renamed to "Is an error", their enclosing contexts already naming the condition); the deferred item is carried out and removed. Behavior-neutral (suite green, 64)
