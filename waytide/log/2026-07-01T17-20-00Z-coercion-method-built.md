# Built: the `Constant()` coercion method as the opt-in `Constant::Coerce` Kernel refinement — an idempotent, type-guarded passthrough to `Constant.build` (`build` unchanged). Four outcomes: coerce a module, idempotency (already-Constant returned unchanged), namespaced-name coercion, and un-coercible value → `TypeError: can't convert nil into Constant` (mirroring `Integer(nil)`). README documents it under Construction; loop record at agent/loops/2026-07-01T16-38-52Z-constant-coercion-method.md. Resolves and removes the constant-coercion-method deferred item. Suite green (68)
