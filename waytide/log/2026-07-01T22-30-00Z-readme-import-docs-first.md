# Reordered the README to lead with `Constant::Import`. The `## Constant::Import` section (and all its subsections — Example, Importing a Constant, Macro/API) was moved above `## The Constant Class`, directly under the intro — which already frames the aliasing/import purpose, so it flows naturally into the Import docs. The `Constant` class/module documentation (Getting a Constant, Coercion, Queries) now follows. `## Defining a Constant` (still "To Do") and `## License` stay at the end. Content-preserving move — 250 lines unchanged, docs only, no code or test impact. Resolves the readme-import-docs-first deferred item
