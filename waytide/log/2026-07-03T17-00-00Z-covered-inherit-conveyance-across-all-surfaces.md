# Covered `inherit` conveyance across every surface that carries it — closing the first sub-item (Tier 1, item 1) of the coverage-discrimination-gaps deferred item. `inherit` had been completely untested: hard-coding it, dropping the `inherit ||= false` coalescing, or failing to convey it through the nested-path recursion would all have kept the suite verified. The coverage is **green-on-arrival** — the library already conveyed `inherit` correctly, so these tests protect existing behavior rather than design new behavior (per the "TDD designs, coverage protects" rule); they were still designed through the test hinges, but with no manufactured red. The discrimination comes from a new **`ancestor:` control** on `Controls::Constant.example` (nested-style spec `{ "SomeAncestor" => ["SomeInheritedConstant"] }`: it builds a named ancestor module holding the inner constants and `include`s it into the subject), which yields a constant reachable **only** via ancestry — resolvable/visible with `inherit: true`, absent with `inherit: false`. Surfaces covered, each as a `When inherit is true` / `When inherit is false` pair: `Constant.get` (class-level, exercising delegation through `Constant::Module#get`), `Constant::Module#constants`, `#constant_names`, class-level `Constant.defined?`, and instance `Constant::Module#defined?` (module form — the branch that conveys `inherit` via `value.constants(inherit)`; the name form merely delegates to `Constant.defined?`, covered transitively). Test structure settled on condition contexts with unnamed `test` blocks (over the `defined_predicate` suite's named Affirms/Refutes convention) for consistency across the whole inherit-gap coverage. Adding the `ancestor:` helper also surfaced a latent fragility unrelated to inherit: the two `include_literals` tests asserted an **unspecified** `Module#constants` ordering, and interning the helper's new symbols at load time deterministically flipped that order (10/10 fail); fixed by making those two assertions order-independent (`5f6dab5`), with the library left untouched. Commits: methodology rules `6c10c46`, order-fix `5f6dab5`, `get` + `ancestor:` helper `b6f9406`, `#constants` `f1ded85`, `defined?` class+instance `6974a32`, `#constant_names` `8f220ab`. Suite verified (85 tests). Resolves the inherit sub-item; coverage-discrimination-gaps items 2 (error-message assertions) and 3 (`Define` returns a module), plus Tiers 2–3, remain open.
