# Dropped the terminology near-duplicate consolidation task — the developer decided not to merge the overlapping rules (`controls-not-factories`/`controls-not-fixtures`/`say-control-not-arrange`; `actuation-not-call`/`tdd-efferent-not-caller`/`tdd-lexicon`) that the rule-categorization log flagged as a possible later item; each rule stays as its own file, and this closes the loose end so it is not resurrected as future work.
